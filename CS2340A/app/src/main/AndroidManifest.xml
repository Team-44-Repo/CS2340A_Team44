<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CS2340A"
        tools:targetApi="31">
        <activity
            android:name=".dungenCrawler.viewModel.LeaderboardViewModel"
            android:exported="false" />
        <activity
            android:name=".dungenCrawler.viewModel.GameRoom3ViewModel"
            android:exported="false" />
        <activity
            android:name=".dungenCrawler.viewModel.GameRoom2ViewModel"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".dungenCrawler.viewModel.WelcomeActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".dungenCrawler.viewModel.EndViewModel"
            android:exported="false" />
        <activity
            android:name=".dungenCrawler.viewModel.GameRoom1ViewModel"
            android:exported="false" />
        <activity
            android:name=".dungenCrawler.viewModel.InitialConfigViewModel"
            android:exported="false" />
        <activity
            android:name=".dungenCrawler.viewModel.MainViewModel"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>